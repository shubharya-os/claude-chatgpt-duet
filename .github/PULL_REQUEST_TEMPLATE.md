**What this changes, and why**

<!-- One or two sentences. What was wrong, or what could not be done before. -->

**How you know it works**

<!-- The gate you ran, and its output. `pytest -q` is the project's own. -->

```
```

**Does the test fail without the fix?**

<!-- This project's own rule, applied to itself: a test that cannot fail is worse
     than no test, because it looks like proof and is not. If you added one, say
     that you watched it fail first — `git stash` the fix and run it. If this
     change needs no test, say why. -->

**Anything you are unsure about**

<!-- Genuinely optional, and genuinely useful. A reviewer who knows where you are
     unsure reads that part harder. -->
